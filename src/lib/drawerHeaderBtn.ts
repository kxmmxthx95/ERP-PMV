/**
 * Icon buttons in DrawerHeader (close / back).
 * Canonical = MorningRollCallWidget drawer — change here → all drawer header icons follow.
 */
export const DRAWER_HEADER_ICON_BTN =
  'inline-flex h-9 w-9 items-center justify-center rounded-full border border-slate-200 bg-white text-slate-600 hover:bg-slate-50 active:scale-[0.98] transition shrink-0';

/** Right cluster for close (+ optional back). Place inside a `relative` header row. */
export const DRAWER_HEADER_RIGHT_ACTIONS =
  'absolute right-0 top-1/2 -translate-y-1/2 shrink-0 flex items-center gap-2';

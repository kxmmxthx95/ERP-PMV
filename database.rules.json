{
  "rules": {
    "wordGames": {
      "$roomCode": {
        ".read": "auth != null && root.child('wordGames').child($roomCode).child('players').child(auth.uid).exists()",
        ".write": false
      }
    }
  }
}
